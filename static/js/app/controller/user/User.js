define([
    'app/controller/base',
], function(base) {

    init();
    
    // 初始化页面
    function init() {
        $("#userNav li").eq(0).addClass("active");
        
        
        addListener();
    }

    function addListener() {
    	
    }
});
